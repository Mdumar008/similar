{
  "name": "similar",
  "version": "1.0.0",
  "description": "A One Hospitality Services – static site with no-code admin",
  "scripts": {
    "start": "live-server --port=8080 --open=./index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
